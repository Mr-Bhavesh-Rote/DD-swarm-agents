# Pinned dependencies (Python 3.11+ required: Langfuse + LangGraph constraint)

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20
sse-starlette==2.1.3

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0          # required by pydantic EmailStr (auth endpoints)
PyYAML==6.0.2

# --- Orchestration / LLM ---
langgraph==0.2.61
langgraph-checkpoint-postgres==2.0.9
langchain==0.3.14                # required by the Langfuse LangChain CallbackHandler
langchain-core==0.3.28
langchain-anthropic==0.3.1
anthropic==0.42.0

# --- Observability ---
langfuse==3.0.3

# --- Database ---
SQLAlchemy==2.0.36
greenlet==3.1.1                 # required by SQLAlchemy async engine
alembic==1.14.0
asyncpg==0.30.0
psycopg[binary]==3.2.3

# --- Jobs / cache ---
redis==5.2.1
rq==2.0.0

# --- Tools / search / scrape ---
tavily-python==0.5.0
httpx==0.28.1
beautifulsoup4==4.12.3
trafilatura==2.0.0

# --- Export ---
weasyprint==63.1
python-docx==1.1.2
markdown==3.7

# --- Auth ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- Test ---
pytest==8.3.4
pytest-asyncio==0.25.0
